---
phase: 08-design-system-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/src/app.css
autonomous: true
requirements:
  - UI-01
  - UI-05
must_haves:
  truths:
    - "Glass-bg alpha is 0.76 (not 0.97) so glass effect is visible through cards"
    - "body::before creates a subtle radial gradient visible behind glass surfaces"
    - "6 new glass tokens exist and have values in :root"
    - "Browsers without backdrop-filter show solid fallback backgrounds"
    - "Users with prefers-reduced-transparency get no blur effects"
  artifacts:
    - path: "apps/web/src/app.css"
      provides: "Glass tokens, body::before, @supports fallback, prefers-reduced-transparency"
      contains: "--glass-bg-subtle"
  key_links:
    - from: "apps/web/src/app.css :root"
      to: ".card, .stat-card, .dialog-content"
      via: "CSS custom property inheritance"
      pattern: "--glass-bg.*rgba.*0\\.7[0-9]"
---

<objective>
Overhaul the glass token system in app.css to enable real glassmorphism — reduce alpha from 0.97 to 0.76, add 6 new glass tokens, add body::before gradient backdrop, and add @supports / prefers-reduced-transparency fallbacks.

Purpose: The current --glass-bg at 0.97 opacity makes glass effects invisible. This plan fixes the foundation so all downstream theme and component work has real blur visibility.
Output: Updated :root block in app.css with corrected glass values, new tokens, body::before rule, and accessibility fallbacks.
</objective>

<execution_context>
@/Users/sebastianzabel/git/clokr/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sebastianzabel/git/clokr/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-design-system-foundation/08-CONTEXT.md
@.planning/phases/08-design-system-foundation/08-UI-SPEC.md

<interfaces>
<!-- Key existing tokens in :root/pflaume block (lines 87-198 of app.css) that Task 1 modifies -->
From apps/web/src/app.css :root block:
```css
/* Current glass tokens (lines 178-182) — these get replaced */
--glass-bg: rgba(255, 255, 255, 0.97);
--glass-bg-strong: rgba(255, 255, 255, 0.99);
--glass-border: rgba(128, 55, 123, 0.08);
--glass-blur: 20px;
--glass-shadow: 0 8px 32px rgba(128, 55, 123, 0.08), 0 2px 8px rgba(128, 55, 123, 0.04);
```

<!-- Consumers of glass tokens — these must continue to work -->
```css
/* .card (line 760) */
background: var(--glass-bg);
border: 1px solid var(--glass-border);
backdrop-filter: blur(var(--glass-blur));
box-shadow: var(--glass-shadow);

/* .stat-card (line 959) */
background: var(--glass-bg);

/* .dialog-content (line 1629) */
background: var(--glass-bg-strong, var(--color-surface));
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add 6 new glass tokens, reduce glass-bg alpha, add body::before gradient backdrop</name>
  <files>apps/web/src/app.css</files>
  <read_first>
    - apps/web/src/app.css (full file — understand :root block at lines 87-198, glass tokens at lines 178-182)
    - .planning/phases/08-design-system-foundation/08-UI-SPEC.md (glass token values for lila theme, body::before gradient spec)
  </read_first>
  <action>
In apps/web/src/app.css, within the `:root, [data-theme="pflaume"]` block (lines 87-198):

1. **Replace the existing glass tokens** (lines 177-182) with these exact values:

```css
/* Glass surfaces */
--glass-bg: rgba(255, 255, 255, 0.76);
--glass-bg-subtle: rgba(255, 255, 255, 0.55);
--glass-bg-overlay: rgba(250, 249, 247, 0.90);
--glass-saturate: 160%;
--glass-highlight: rgba(255, 255, 255, 0.60);
--glass-border: rgba(128, 55, 123, 0.10);
--glass-border-light: rgba(255, 255, 255, 0.50);
--glass-shadow: 0 8px 32px rgba(128, 55, 123, 0.10), 0 2px 8px rgba(128, 55, 123, 0.05);
--glass-shadow-inset: inset 0 1px 0 rgba(255, 255, 255, 0.60);
--glass-blur: 16px;
```

Remove `--glass-bg-strong` (no longer used in new system; consumers like `.dialog-content` already have `var(--color-surface)` fallback).

2. **Add body::before gradient backdrop** — insert this rule AFTER the `:root` block closing brace and BEFORE the `[data-theme="nacht"]` block (insert around line 199):

```css
/* ─── Body Gradient Backdrop (Glassmorphism visibility layer) ────────── */
body::before {
  content: '';
  position: fixed;
  inset: 0;
  pointer-events: none;
  z-index: -1;
  background: radial-gradient(ellipse at 80% 10%, rgba(168, 92, 163, 0.10) 0%, transparent 60%);
}
```

3. **Add smooth theme-switch transition** to `:root` — add inside the `:root` block (before the closing brace):

```css
/* Theme switch transition */
transition: color 200ms ease, background-color 200ms ease;
```

Do NOT modify any of the existing glass token consumers (.card, .stat-card, .dialog-content, etc.) in this task — they will naturally pick up the new values through CSS custom property inheritance.

Do NOT touch the nacht/wald/schiefer/pro theme blocks — those are handled in Plan 02.
  </action>
  <verify>
    <automated>cd /Users/sebastianzabel/git/clokr && grep -c "glass-bg-subtle\|glass-bg-overlay\|glass-saturate\|glass-highlight\|glass-border-light\|glass-shadow-inset" apps/web/src/app.css | grep -q "^6$" && grep "glass-bg: rgba(255, 255, 255, 0.76)" apps/web/src/app.css && grep "body::before" apps/web/src/app.css && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - apps/web/src/app.css contains `--glass-bg: rgba(255, 255, 255, 0.76)` (not 0.97)
    - apps/web/src/app.css contains `--glass-bg-subtle: rgba(255, 255, 255, 0.55)`
    - apps/web/src/app.css contains `--glass-bg-overlay: rgba(250, 249, 247, 0.90)`
    - apps/web/src/app.css contains `--glass-saturate: 160%`
    - apps/web/src/app.css contains `--glass-highlight: rgba(255, 255, 255, 0.60)`
    - apps/web/src/app.css contains `--glass-border-light: rgba(255, 255, 255, 0.50)`
    - apps/web/src/app.css contains `--glass-shadow-inset: inset 0 1px 0 rgba(255, 255, 255, 0.60)`
    - apps/web/src/app.css contains `--glass-blur: 16px` (changed from 20px)
    - apps/web/src/app.css contains `body::before` rule with `radial-gradient(ellipse at 80% 10%, rgba(168, 92, 163, 0.10)`
    - apps/web/src/app.css contains `pointer-events: none` inside body::before rule
    - apps/web/src/app.css contains `z-index: -1` inside body::before rule
    - apps/web/src/app.css does NOT contain `--glass-bg-strong` in the :root block
    - apps/web/src/app.css :root block contains `transition: color 200ms ease, background-color 200ms ease`
  </acceptance_criteria>
  <done>
    :root block has 10 glass tokens (replacing 5 old ones), body::before gradient backdrop exists, theme transition added. The existing .card/.stat-card/.dialog-content consumers work unchanged via CSS variable inheritance.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add @supports fallback and prefers-reduced-transparency media query</name>
  <files>apps/web/src/app.css</files>
  <read_first>
    - apps/web/src/app.css (check existing @supports rules, @media rules for prefers-reduced-motion around end of file)
    - .planning/phases/08-design-system-foundation/08-UI-SPEC.md (fallback and accessibility contract sections)
  </read_first>
  <action>
In apps/web/src/app.css, add two new rule blocks. Place them AFTER the `.card` rule (after line ~771, after `.card:hover`) and BEFORE the `.card-header` rule:

1. **@supports fallback** — for browsers that don't support backdrop-filter:

```css
/* ─── Glass Fallback: no backdrop-filter support ─────────────────────── */
@supports not (backdrop-filter: blur(1px)) {
  .card,
  .stat-card,
  .table-wrapper,
  .dialog-content {
    background: var(--color-surface);
    backdrop-filter: none;
    -webkit-backdrop-filter: none;
  }
  body::before {
    display: none;
  }
}
```

2. **prefers-reduced-transparency** — for users who prefer reduced transparency:

```css
/* ─── Accessibility: reduced transparency preference ─────────────────── */
@media (prefers-reduced-transparency: reduce) {
  .card,
  .stat-card,
  .table-wrapper,
  .dialog-content {
    backdrop-filter: none;
    -webkit-backdrop-filter: none;
    background: var(--color-surface);
  }
  body::before {
    display: none;
  }
}
```

These two blocks handle D-12 (@supports fallback) and D-13 (prefers-reduced-transparency) from CONTEXT.md.
  </action>
  <verify>
    <automated>cd /Users/sebastianzabel/git/clokr && grep "@supports not (backdrop-filter: blur(1px))" apps/web/src/app.css && grep "prefers-reduced-transparency: reduce" apps/web/src/app.css && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - apps/web/src/app.css contains `@supports not (backdrop-filter: blur(1px))`
    - apps/web/src/app.css contains `@media (prefers-reduced-transparency: reduce)`
    - Both blocks set `backdrop-filter: none` and `-webkit-backdrop-filter: none` on `.card, .stat-card, .table-wrapper, .dialog-content`
    - Both blocks set `background: var(--color-surface)` as solid fallback
    - Both blocks set `body::before { display: none; }` to hide gradient
    - The @supports block appears AFTER the .card rule and BEFORE .card-header
  </acceptance_criteria>
  <done>
    Browsers without backdrop-filter support show solid backgrounds. Users with prefers-reduced-transparency get no blur/glass effects. Both fallbacks hide the body::before gradient.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None new | CSS token changes only — no user input processing, no API calls, no auth changes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01 | Tampering | CSS custom properties | accept | CSS variables are not user-controllable — they are defined in static CSS files served by the build system. No user input flows into CSS variable values. |
| T-08-02 | Information Disclosure | body::before gradient | accept | Gradient is purely decorative, contains no data. Fixed position with pointer-events:none prevents interaction. |
</threat_model>

<verification>
1. Run `grep -c "glass-bg-subtle" apps/web/src/app.css` — expect count >= 1
2. Run `grep "body::before" apps/web/src/app.css` — body::before rule exists
3. Run `grep "@supports not" apps/web/src/app.css` — fallback block exists
4. Run `grep "prefers-reduced-transparency" apps/web/src/app.css` — a11y query exists
5. Run `pnpm --filter @clokr/web build` — build succeeds with no CSS errors
</verification>

<success_criteria>
- :root block has 10 glass tokens replacing 5 old ones, with --glass-bg alpha at 0.76
- body::before gradient backdrop exists with pointer-events:none and z-index:-1
- @supports not (backdrop-filter) fallback provides solid backgrounds
- prefers-reduced-transparency media query disables all blur
- Theme switch transition (200ms ease) on :root
- Web app builds successfully
</success_criteria>

<output>
After completion, create `.planning/phases/08-design-system-foundation/08-01-SUMMARY.md`
</output>
