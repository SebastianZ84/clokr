---
phase: 06-pdf-exports-monatsbericht-urlaubsliste-abteilungsbericht
plan: 02
type: execute
wave: 2
depends_on:
  - 06-01
files_modified:
  - apps/web/src/routes/(app)/reports/+page.svelte
autonomous: false
requirements:
  - PDF-01
  - PDF-02
  - PDF-03
user_setup: []
must_haves:
  truths:
    - "The /reports page displays two new cards: 'Firmenweiter Monatsbericht' (company monthly PDF with month/year/role selectors) and 'Urlaubsliste' (year-based vacation periods PDF)"
    - "Clicking 'PDF herunterladen' on the company monthly card downloads a file named 'Monatsbericht_Alle_{year}_{month}.pdf'"
    - "The role selector offers exactly three options: 'Alle Mitarbeiter' (all), 'Nur Mitarbeiter' (EMPLOYEE), 'Nur Manager' (MANAGER)"
    - "Clicking 'PDF herunterladen' on the Urlaubsliste card downloads a file named 'Urlaubsliste_{year}.pdf'"
    - "Both cards show loading state ('Vorbereiten…' + spinner) during download"
    - "Both cards show German error messages on failure in an .alert.alert-error block"
    - "Page header subtitle mentions the new PDF options"
  artifacts:
    - path: "apps/web/src/routes/(app)/reports/+page.svelte"
      provides: "Two new report-card blocks + downloadCompanyMonthlyPdf() + downloadLeaveListPdf() handlers"
      contains: "Firmenweiter Monatsbericht"
  key_links:
    - from: "downloadCompanyMonthlyPdf()"
      to: "GET /api/v1/reports/monthly/pdf/all"
      via: "downloadPdf() utility with month, year, role query params"
      pattern: "/reports/monthly/pdf/all"
    - from: "downloadLeaveListPdf()"
      to: "GET /api/v1/reports/leave-list/pdf"
      via: "downloadPdf() utility with year query param"
      pattern: "/reports/leave-list/pdf"
---

<objective>
Surface the two new backend PDF endpoints (added in Plan 06-01) to users by adding two new cards to the existing reports page. Managers and admins need a visible, clickable UI to trigger the company monthly PDF (with role filter) and the yearly vacation-list PDF.

Purpose: Backend endpoints without UI are invisible to users. This plan wires the reports page to Plan 06-01's endpoints, following the existing `.report-card` pattern, CSS custom properties (theme-aware), German labels, Svelte 5 runes, and the `card-animate` entrance animation convention from CLAUDE.md.

Output:
- Two new `.report-card` blocks in `.reports-grid`
- Two new download handlers using the existing `downloadPdf()` utility
- State variables using `$state()` runes (loading, error, month, year, role)
- Updated page subtitle to mention the new reports
</objective>

<execution_context>
@/Users/sebastianzabel/git/clokr/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sebastianzabel/git/clokr/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/06-pdf-exports-monatsbericht-urlaubsliste-abteilungsbericht/06-CONTEXT.md
@.planning/phases/06-pdf-exports-monatsbericht-urlaubsliste-abteilungsbericht/06-RESEARCH.md
@.planning/phases/06-pdf-exports-monatsbericht-urlaubsliste-abteilungsbericht/06-01-PLAN.md
@.planning/phases/06-pdf-exports-monatsbericht-urlaubsliste-abteilungsbericht/06-01-SUMMARY.md
@CLAUDE.md
@apps/web/src/routes/(app)/reports/+page.svelte

<interfaces>
<!-- Existing utilities in the current +page.svelte — reuse as-is -->

```typescript
// Existing state pattern:
let leaveYear = $state(currentYear);
let leaveLoading = $state(false);
let leaveError = $state("");

// Existing utility — already defined in the file, reuse directly:
async function downloadPdf(url: string, filename: string): Promise<void>;
// Usage: await downloadPdf("/reports/leave-overview/pdf?year=2025", "file.pdf");

// Existing constants:
const months: string[] = ["Januar", "Februar", ...]; // 12 German month names
const years: number[] = [currentYear, currentYear - 1, ..., currentYear - 4];

// Existing card CSS classes (defined in the file's <style> block):
// .reports-grid, .report-card, .report-card-icon-section, .report-card-icon-section--purple,
// .report-card-icon-section--green, .report-card-icon-section--blue, .report-icon-lg,
// .report-card-header, .report-card-title, .report-card-desc, .report-controls,
// .form-group, .form-label, .form-input, .btn, .btn-primary, .btn-spinner,
// .alert, .alert-error
```

Backend endpoints (from Plan 06-01):
```
GET /api/v1/reports/monthly/pdf/all?year={y}&month={m}&role={all|EMPLOYEE|MANAGER}
  → 200 application/pdf
  → 401 if no auth
  → 403 if EMPLOYEE token
  → 404 { error: "Keine Mitarbeiter gefunden" } if filter yields 0

GET /api/v1/reports/leave-list/pdf?year={y}
  → 200 application/pdf
  → 401 if no auth
  → 403 if EMPLOYEE token
```

From CLAUDE.md UI Consistency Rules (must follow):
- Entrance animations: every primary content block MUST have `class="... card-animate"` — check if `.reports-grid` children already have it in existing code; match that pattern
- Theme-aware colors: NEVER hardcode hex in `<style>` blocks; use `var(--color-*)` / `var(--color-brand-tint)` / `var(--color-green-bg)` / `var(--color-blue-bg)` etc.
- Glass surfaces: top-level cards should use `var(--glass-bg)` etc. — but the existing `.report-card` extends `.card.card-body` which already handles this
- German UI text
- Svelte 5 runes: `$state`, `$derived`, `$effect`

</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add 'Firmenweiter Monatsbericht' card + 'Urlaubsliste' card + download handlers</name>
  <files>apps/web/src/routes/(app)/reports/+page.svelte</files>
  <action>
    1. Read the current apps/web/src/routes/(app)/reports/+page.svelte in full (it is ~534 lines — read all).

    2. In the `<script lang="ts">` section, add new state variables after the existing `leaveError` block (around line 41):
       ```typescript
       // Company monthly PDF (Firmenweiter Monatsbericht) — PDF-01 / PDF-03
       let companyPdfMonth = $state(currentMonth);
       let companyPdfYear = $state(currentYear);
       let companyPdfRole = $state<"all" | "EMPLOYEE" | "MANAGER">("all");
       let companyPdfLoading = $state(false);
       let companyPdfError = $state("");

       // Urlaubsliste PDF (individual leave periods) — PDF-02
       let leaveListYear = $state(currentYear);
       let leaveListLoading = $state(false);
       let leaveListError = $state("");
       ```

    3. Add two new download handler functions after the existing `downloadLeaveOverviewPdf` function (around line 173):
       ```typescript
       async function downloadCompanyMonthlyPdf() {
         companyPdfLoading = true;
         companyPdfError = "";
         try {
           await downloadPdf(
             `/reports/monthly/pdf/all?month=${companyPdfMonth}&year=${companyPdfYear}&role=${companyPdfRole}`,
             `Monatsbericht_Alle_${companyPdfYear}_${String(companyPdfMonth).padStart(2, "0")}.pdf`,
           );
         } catch (e: unknown) {
           companyPdfError = e instanceof Error ? e.message : "PDF-Download fehlgeschlagen";
         } finally {
           companyPdfLoading = false;
         }
       }

       async function downloadLeaveListPdf() {
         leaveListLoading = true;
         leaveListError = "";
         try {
           await downloadPdf(
             `/reports/leave-list/pdf?year=${leaveListYear}`,
             `Urlaubsliste_${leaveListYear}.pdf`,
           );
         } catch (e: unknown) {
           leaveListError = e instanceof Error ? e.message : "PDF-Download fehlgeschlagen";
         } finally {
           leaveListLoading = false;
         }
       }
       ```

    4. Update the page header subtitle (line ~195) from:
       ```html
       <p>Monatsberichte und DATEV-Exporte erstellen</p>
       ```
       to:
       ```html
       <p>Monatsberichte, Urlaubslisten und DATEV-Exporte erstellen</p>
       ```

    5. In the `.reports-grid` container (starts line ~198), add TWO NEW `.report-card` blocks AFTER the existing "Leave Overview PDF Card" (which ends around line 328) but BEFORE the closing `</div>` of `.reports-grid`.

       Card 1 — Firmenweiter Monatsbericht (PDF-01 / PDF-03):
       ```html
       <!-- Company Monthly PDF Card (PDF-01 / PDF-03) -->
       <div class="card card-body report-card">
         <div class="report-card-icon-section report-card-icon-section--purple">
           <span class="report-icon-lg">📑</span>
         </div>
         <div class="report-card-header">
           <div>
             <h2 class="report-card-title">Firmenweiter Monatsbericht</h2>
             <p class="report-card-desc text-muted">
               Alle Mitarbeiter in einer PDF — optional nach Rolle gefiltert
             </p>
           </div>
         </div>

         <div class="report-controls">
           <div class="form-group">
             <label class="form-label" for="company-pdf-month">Monat</label>
             <select id="company-pdf-month" bind:value={companyPdfMonth} class="form-input">
               {#each months as name, i (i)}
                 <option value={i + 1}>{name}</option>
               {/each}
             </select>
           </div>
           <div class="form-group">
             <label class="form-label" for="company-pdf-year">Jahr</label>
             <select id="company-pdf-year" bind:value={companyPdfYear} class="form-input">
               {#each years as y (y)}
                 <option value={y}>{y}</option>
               {/each}
             </select>
           </div>
         </div>

         <div class="form-group">
           <label class="form-label" for="company-pdf-role">Rolle</label>
           <select id="company-pdf-role" bind:value={companyPdfRole} class="form-input">
             <option value="all">Alle Mitarbeiter</option>
             <option value="EMPLOYEE">Nur Mitarbeiter</option>
             <option value="MANAGER">Nur Manager</option>
           </select>
         </div>

         <button
           class="btn btn-primary"
           onclick={downloadCompanyMonthlyPdf}
           disabled={companyPdfLoading}
         >
           {#if companyPdfLoading}
             <span class="btn-spinner"></span>
             Vorbereiten…
           {:else}
             PDF herunterladen
           {/if}
         </button>

         {#if companyPdfError}
           <div class="alert alert-error" role="alert">
             <span>⚠</span>
             <span>{companyPdfError}</span>
           </div>
         {/if}
       </div>
       ```

       Card 2 — Urlaubsliste (PDF-02):
       ```html
       <!-- Leave List PDF Card (PDF-02) — individual periods, not entitlement summary -->
       <div class="card card-body report-card">
         <div class="report-card-icon-section report-card-icon-section--blue">
           <span class="report-icon-lg">🗓</span>
         </div>
         <div class="report-card-header">
           <div>
             <h2 class="report-card-title">Urlaubsliste PDF</h2>
             <p class="report-card-desc text-muted">
               Einzelne Urlaubszeiträume aller Mitarbeiter als PDF
             </p>
           </div>
         </div>

         <div class="report-controls">
           <div class="form-group">
             <label class="form-label" for="leave-list-year">Jahr</label>
             <select id="leave-list-year" bind:value={leaveListYear} class="form-input">
               {#each years as y (y)}
                 <option value={y}>{y}</option>
               {/each}
             </select>
           </div>
         </div>

         <button
           class="btn btn-primary"
           onclick={downloadLeaveListPdf}
           disabled={leaveListLoading}
         >
           {#if leaveListLoading}
             <span class="btn-spinner"></span>
             Vorbereiten…
           {:else}
             PDF herunterladen
           {/if}
         </button>

         {#if leaveListError}
           <div class="alert alert-error" role="alert">
             <span>⚠</span>
             <span>{leaveListError}</span>
           </div>
         {/if}
       </div>
       ```

    6. DO NOT add any new CSS to the `<style>` block — both new cards reuse the existing `.report-card`, `.report-controls`, `.form-group`, etc. classes. This is the whole point of the existing pattern.

    7. CSS color compliance check: per CLAUDE.md, the existing `.report-card-icon-section--purple` uses `var(--color-brand-tint, rgba(124, 58, 237, 0.1))` as a fallback. The new cards reuse these existing classes — NO new hex colors introduced.

    8. Use the Svelte MCP server to validate the file:
       - Call `mcp__svelte__svelte-autofixer` with the file path
       - Fix any issues reported (for example, Svelte 5 does not allow `on:click` — must use `onclick={handler}`; the existing file already uses this pattern so the new code should follow suit)
       - Re-run the autofixer until no issues are reported

    9. Run the project type check:
       `pnpm --filter @clokr/web check`
       (SvelteKit's svelte-check command — validates TS + Svelte)

    10. Svelte 5 gotcha reminders (from CLAUDE.md):
        - `{@const}` inside `<div>` is NOT allowed — only inside `{#if}` / `{#each}` / `{#snippet}`. The new code does not need `{@const}` at all.
        - Use `$state()` for local reactive state — the new variables use this
        - Event handlers: `onclick={fn}` (NOT `on:click={fn}`)
        - The existing file already uses Svelte 5 runes — match its style exactly
  </action>
  <verify>
    <automated>pnpm --filter @clokr/web check</automated>
  </verify>
  <done>
    - Two new `.report-card` blocks exist in `.reports-grid`
    - State variables `companyPdfMonth/Year/Role/Loading/Error` and `leaveListYear/Loading/Error` exist in the script
    - `downloadCompanyMonthlyPdf()` and `downloadLeaveListPdf()` functions exist and use the existing `downloadPdf()` utility
    - `pnpm --filter @clokr/web check` passes with no Svelte or TypeScript errors
    - Svelte MCP autofixer reports no issues
    - Page subtitle mentions "Urlaubslisten"
    - No new hex colors hardcoded in the `<style>` block
    - No new CSS rules added — both cards reuse existing classes
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verification — visual + functional smoke test of new cards</name>
  <files>apps/web/src/routes/(app)/reports/+page.svelte</files>
  <what-built>
    Two new cards on the /reports page:
    1. "Firmenweiter Monatsbericht" with Monat/Jahr/Rolle selectors and a PDF download button
    2. "Urlaubsliste PDF" with a Jahr selector and a PDF download button

    Both download from the new backend endpoints added in Plan 06-01.
  </what-built>
  <action>
    This is a human verification checkpoint. Claude should NOT modify files in this task.
    Instead, Claude presents the verification instructions (below) to the user and waits for
    the resume signal ("approved" or a description of problems).

    The automated work is already complete (Task 1). This checkpoint exists because:
    - Visual style / theme correctness can only be assessed by a human viewing the rendered page
    - PDF content validity can only be confirmed by opening the downloaded file in a PDF viewer
    - End-to-end functional flows (login → navigate → click → download → inspect) are impractical to automate for PDF binaries

    Claude should:
    1. Verify that Task 1 passed its `pnpm --filter @clokr/web check` automated verification
    2. Present the `<how-to-verify>` steps to the user
    3. Pause and wait for the user's resume signal
    4. If the user reports problems, diagnose and propose fixes as auto follow-up tasks
  </action>
  <how-to-verify>
    1. Make sure the dev stack is running via Docker: `docker compose up --build -d`
    2. Open the web app in a browser: http://localhost:5173 (or whichever port the dev compose exposes)
    3. Log in as an admin user
    4. Navigate to `/reports`
    5. Verify the page shows FIVE cards in the grid (was 3, now 5):
       - Monatsbericht (existing)
       - DATEV Export (existing)
       - Urlaubsübersicht PDF (existing)
       - Firmenweiter Monatsbericht (NEW)
       - Urlaubsliste PDF (NEW)
    6. Verify the page subtitle says "Monatsberichte, Urlaubslisten und DATEV-Exporte erstellen"
    7. Visual check: new cards look identical in style to existing ones (padding, colors, button, icon tile). No raw hex colors bleeding through. Theme switching (if multiple themes exist) still applies cleanly.
    8. On the new "Firmenweiter Monatsbericht" card:
       - Select a recent Monat/Jahr where time entries exist
       - Select Rolle = "Alle Mitarbeiter"
       - Click "PDF herunterladen"
       - Verify: button shows "Vorbereiten…" + spinner briefly, then a file downloads
       - Verify: filename matches `Monatsbericht_Alle_{year}_{MM}.pdf`
       - Open the PDF in the browser's PDF viewer
       - Verify: tenant name appears on a colored band at the top of page 1
       - Verify: a summary table lists all employees with Soll/Ist/Saldo/Urlaub/Krank columns
       - Verify: subsequent pages contain per-employee details
    9. Repeat with Rolle = "Nur Mitarbeiter" — PDF should contain fewer (or same) employees
    10. Repeat with Rolle = "Nur Manager" — either a PDF with only managers, or an error if no managers exist
    11. On the new "Urlaubsliste PDF" card:
        - Select a year
        - Click "PDF herunterladen"
        - Verify: filename matches `Urlaubsliste_{year}.pdf`
        - Open the PDF
        - Verify: tenant name on colored header band
        - Verify: employees with approved leave in that year are listed with individual period sub-tables (Von / Bis / Typ / Tage)
    12. Error path check (optional but recommended):
        - Open DevTools network tab
        - On one of the new cards, disable the network briefly and click download
        - Verify: a German error message appears in `.alert.alert-error` (e.g. "PDF-Download fehlgeschlagen")
    13. PDF-04 regression check: on the EXISTING "Monatsbericht" card, load the monthly report, then click "PDF" next to any employee row in the results table. The per-employee PDF should now also show the colored header band + "Seite X von Y" footer (proves PDF-04 works end-to-end from the UI).
  </how-to-verify>
  <verify>
    Manual human verification — all 13 steps in `<how-to-verify>` must pass.
    No automated command runs at this checkpoint; Task 1's automated verify already ran.
  </verify>
  <done>
    User has typed "approved" after completing all 13 verification steps, OR
    User has reported specific issues which have been fixed in follow-up auto tasks and re-verified.
  </done>
  <resume-signal>
    Type "approved" if all 13 steps pass. Otherwise describe what went wrong (broken layout, missing card, failing download, wrong filename, missing tenant branding in PDF, etc.) — the executor will fix and re-verify.
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → Web app (SvelteKit SSR) | No new trust boundary — frontend only constructs URLs from local $state variables |
| Web app → Fastify API | Existing `downloadPdf()` utility injects the Bearer token; new handlers reuse it unchanged |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-10 | Tampering — XSS via German error messages | `.alert.alert-error` blocks on new cards | mitigate | Error text flows into `{companyPdfError}` / `{leaveListError}` via Svelte's `{}` interpolation, which auto-escapes — no `{@html}` used. |
| T-06-11 | Information Disclosure — role filter exposes role-label in PDF | Company PDF role selector | accept | The role filter is not sensitive information; only ADMIN/MANAGER can access the /reports page at all (existing layout guard). |
| T-06-12 | Elevation of Privilege — bypass via direct URL | New /reports page route | mitigate | /reports is already gated by the `(app)` layout group auth guard. No new guard needed — the new cards only expose buttons that call already-protected endpoints. |
| T-06-13 | Tampering — CSRF on download buttons | Download GET requests | accept | Plain GETs with Bearer token in Authorization header; the API requires the bearer token so a cross-site link in another origin cannot forge the request (no cookie-based auth involved). Consistent with existing /reports behavior. |
</threat_model>

<verification>
- `pnpm --filter @clokr/web check` — passes with no Svelte/TS errors
- Svelte MCP autofixer — reports no issues after the edit
- Human verification checkpoint (Task 2) — all 13 steps pass
</verification>

<success_criteria>
- PDF-01 ✅ (end-to-end) — User can click a button on /reports and receive a company-wide monthly PDF
- PDF-02 ✅ (end-to-end) — User can click a button on /reports and receive a yearly vacation-list PDF
- PDF-03 ✅ (end-to-end) — User can select "Alle Mitarbeiter" / "Nur Mitarbeiter" / "Nur Manager" and the resulting PDF is filtered accordingly
- Visual consistency ✅ — New cards match the existing `.report-card` style; no custom CSS added; theme colors applied via existing CSS variables
- German labels ✅ — All user-facing text in German; code/variables in English
- Svelte 5 compliance ✅ — Uses `$state()`, `onclick={fn}`, no `on:click`, no `{@const}` in divs
</success_criteria>

<output>
After completion, create `.planning/phases/06-pdf-exports-monatsbericht-urlaubsliste-abteilungsbericht/06-02-SUMMARY.md` documenting:
- Files modified (apps/web/src/routes/(app)/reports/+page.svelte only)
- New state variables and handlers added
- Confirmation that no new CSS rules were introduced
- Svelte MCP autofixer result
- Human verification result from Task 2 checkpoint
- Any deviations from this plan and why
</output>
